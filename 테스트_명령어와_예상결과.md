# Mini Git 테스트 명령어와 예상 결과

이 문서는 현재 `main.py`의 모든 필수 기능을 순서대로 확인하기 위한 수동 테스트 가이드다.

## 테스트 전 주의사항

- 각 큰 시나리오는 **새 터미널에서 `python main.py`를 다시 실행**해 시작한다.
- 프로그램은 메모리에서만 동작하므로 종료하면 모든 커밋·브랜치가 사라진다.
- 아래 해시 `c000001`, `c000002`, `c000003`은 새 실행 직후 첫 `INIT`부터 시작했을 때의 예상값이다.
- timestamp는 현재 시각이므로 정확한 숫자 대신 `<생성시각>`으로 표시한다.
- 명령어는 대소문자를 구분하지 않지만, 예시는 읽기 쉽게 소문자로 적었다.

---

## 1. 핵심 통합 테스트

### 1-1. 프로그램 실행과 저장소 초기화

터미널에서 실행한다.

```bash
python main.py
```

다음 입력을 순서대로 넣는다.

```text
mini-git> init "Alice"
```

예상 결과:

```text
Initialized repository.
Current branch: main
Current user: Alice
```

확인할 것:

- [ ] `main` 브랜치가 만들어진다.
- [ ] 현재 사용자가 `Alice`가 된다.
- [ ] 아직 커밋은 없다.

### 1-2. 최초 커밋 만들기

```text
mini-git> commit "Initial commit"
```

예상 결과:

```text
[main c000001] Initial commit
```

확인할 것:

- [ ] 첫 해시가 `c000001`이다.
- [ ] 첫 커밋에는 부모가 없다.
- [ ] `main`의 HEAD가 `c000001`을 가리킨다.

### 1-3. 브랜치 생성과 전환

```text
mini-git> branch feature
mini-git> switch feature
```

예상 결과:

```text
Created branch: feature
Switched to branch: feature
```

이제 `feature`와 `main`은 모두 `c000001`을 가리킨다.

```text
main    ──> c000001
feature ──> c000001   ← 현재 브랜치
```

### 1-4. feature 브랜치에 커밋 만들기

```text
mini-git> commit "Add login feature"
```

예상 결과:

```text
[feature c000002] Add login feature
```

이 시점의 관계:

```text
main    ──> c000001
feature ──> c000002

c000002의 부모 = [c000001]
```

### 1-5. main으로 돌아와 다른 커밋 만들기

```text
mini-git> switch main
mini-git> commit "Add payment feature"
```

예상 결과:

```text
Switched to branch: main
[main c000003] Add payment feature
```

최종 그래프:

```text
      c000001: Initial commit
       /                    \
c000002: login        c000003: payment
 feature                   main
```

확인할 것:

- [ ] `c000002`의 부모가 `c000001`이다.
- [ ] `c000003`의 부모도 `c000001`이다.
- [ ] `feature`는 `c000002`, `main`은 `c000003`을 가리킨다.

---

## 2. LOG와 그래프 탐색 테스트

앞의 핵심 통합 테스트를 **종료하지 않고 그대로 이어서** 실행한다.

### 2-1. 전체 LOG

```text
mini-git> log
```

예상 결과 형식:

```text
commit c000001 (Alice, <생성시각>)
Initial commit
commit c000002 (Alice, <생성시각>)
Add login feature
commit c000003 (Alice, <생성시각>)
Add payment feature
```

확인할 것:

- [ ] 분기된 `c000002`와 `c000003`이 모두 출력된다.
- [ ] 부모 `c000001`이 두 자식보다 먼저 출력된다.
- [ ] 각 항목에 hash, author, timestamp, message가 있다.

### 2-2. 조상 탐색

```text
mini-git> ancestors c000002
```

예상 결과 형식:

```text
commit c000001 (Alice, <생성시각>)
Initial commit
```

시작 커밋 자신은 조상에 포함하지 않는다.

```text
mini-git> ancestors c000001
```

예상 결과:

```text
No ancestors
```

### 2-3. 최단 경로

```text
mini-git> path c000002 c000003
```

예상 결과:

```text
Path: c000002 -> c000001 -> c000003
```

`PATH`는 부모·자식 관계를 양방향으로 해석하므로, feature 커밋에서 공통 부모를 거쳐 main 커밋으로 이동할 수 있다.

같은 커밋을 시작·끝으로 지정하는 경우도 확인한다.

```text
mini-git> path c000001 c000001
```

예상 결과:

```text
Path: c000001
```

### 2-4. `No path` 만들기

`No path`은 해시가 존재하지만 두 커밋 사이에 부모·자식 연결이 전혀 없을 때 나온다. 단순히 존재하지 않는 값으로 `path a b`를 입력하면 `No path`가 아니라 `Unknown commit: a`가 나온다.

아래 시나리오는 앞의 통합 테스트와 별개다. 먼저 `quit`으로 종료하고, **새로 `python main.py`를 실행한 뒤** 순서대로 입력한다.

```text
mini-git> init "Alice"
mini-git> branch isolated
mini-git> commit "Main root"
mini-git> switch isolated
mini-git> commit "Isolated root"
mini-git> path c000001 c000002
```

예상 결과:

```text
Initialized repository.
Current branch: main
Current user: Alice
Created branch: isolated
[main c000001] Main root
Switched to branch: isolated
[isolated c000002] Isolated root
No path
```

이 순서가 필요한 이유는 첫 커밋 전에는 두 브랜치의 HEAD가 모두 `None`이기 때문이다.

```text
INIT 직후:
main     -> None
isolated -> None

main에서 첫 커밋:
c000001의 parents = []

isolated에서 첫 커밋:
c000002의 parents = []
```

두 커밋은 모두 부모가 없는 독립된 루트이므로, 부모·자식 연결을 양방향으로 보더라도 갈 수 있는 경로가 없다.

---

## 3. SEARCH와 역색인 테스트

계속 같은 실행 세션에서 진행한다.

### 3-1. 키워드 검색

```text
mini-git> search login
mini-git> search "LOGIN"
```

두 명령 모두 예상 결과:

```text
Found 1 commit:
- c000002: Add login feature
```

확인할 것:

- [ ] 대소문자와 상관없이 `login` 토큰을 찾는다.
- [ ] 커밋 전체를 순회하는 대신 keyword 역색인을 사용한다.

### 3-2. 작성자 검색

```text
mini-git> search --author=Alice
```

예상 결과:

```text
Found 3 commits:
- c000001: Initial commit
- c000002: Add login feature
- c000003: Add payment feature
```

없는 작성자도 확인한다.

```text
mini-git> search --author=Bob
```

예상 결과:

```text
Found 0 commits:
```

---

## 4. LOG 정렬 옵션 테스트

계속 같은 실행 세션에서 진행한다.

```text
mini-git> log --sort-by=date
mini-git> log --sort-by=author
```

두 명령 모두 세 커밋을 출력해야 한다.

```text
commit c000001 (...)
Initial commit
commit c000002 (...)
Add login feature
commit c000003 (...)
Add payment feature
```

이번 핵심 시나리오의 세 커밋은 모두 Alice가 만들고 짧은 시간 안에 생성되므로, 화면상 순서가 기본 LOG와 같을 수 있다. 이는 오류가 아니다.

| 명령 | 확인 가능한 점 | 이 CLI만으로 확인하기 어려운 점 |
|---|---|---|
| `log --sort-by=date` | 옵션 파싱과 날짜 정렬 함수 실행 | 같은 초에 생성되면 눈에 띄는 순서 변화 |
| `log --sort-by=author` | 옵션 파싱과 작성자 정렬 함수 실행 | 한 세션에는 `INIT`으로 설정한 한 명의 author만 있음 |

현재 미션 명령어에는 사용자를 바꾸는 명령이 없고, `INIT`을 다시 실행하면 저장소가 초기화된다. 따라서 서로 다른 작성자가 섞인 정렬 순서를 REPL만으로 만들 수는 없다. 이 경우 코드 내부 테스트 또는 향후 사용자 변경 기능이 필요하다.

잘못된 옵션을 확인한다.

```text
mini-git> log --sort-by=unknown
```

예상 결과:

```text
Invalid args
```

---

## 5. 오류·경계값 테스트

이 시나리오는 앞 테스트가 끝난 뒤 `quit`으로 종료하고, **새로 `python main.py`를 실행한 뒤** 테스트한다.

| 입력 | 예상 결과 | 확인 목적 |
|---|---|---|
| `commit "Before init"` | `Repository not initialized` | 초기화 전 커밋 방지 |
| `init` | `Invalid args` | INIT 인자 검증 |
| `init "Alice" extra` | `Invalid args` | INIT 인자 개수 검증 |
| `branch feature` | `Repository not initialized` | 초기화 전 브랜치 생성 방지 |
| `init "Alice"` | 초기화 성공 메시지 | 이후 오류 테스트 준비 |
| `branch feature` | `Created branch: feature` | 정상 생성 |
| `branch feature` | `Branch already exists: feature` | 중복 브랜치 방지 |
| `switch missing` | `Unknown branch: missing` | 없는 브랜치 처리 |
| `ancestors missing` | `Unknown commit: missing` | 없는 커밋 처리 |
| `path missing c000001` | `Unknown commit: missing` | PATH 해시 검증 |
| `search --author=` | `Invalid args` | 빈 작성자 옵션 검증 |
| `search --unknown=Alice` | `Invalid args` | 지원하지 않는 옵션 검증 |
| `log --sort-by=unknown` | `Invalid args` | 지원하지 않는 정렬 기준 |
| `commit "닫히지 않은 메시지` | `Invalid args` | 따옴표 파싱 오류 처리 |
| `unknown` | `Unknown command: UNKNOWN` | 알 수 없는 명령 처리 |

`path missing c000001`은 두 커밋 모두 존재 여부를 검사하므로, 첫 번째 해시 `missing`에 대한 오류가 먼저 나온다.

## 6. 종료 테스트

```text
mini-git> QUIT
```

또는:

```text
mini-git> exit
```

예상 결과: 추가 오류 메시지 없이 프로그램이 종료된다.

## 최종 체크리스트

- [ ] 핵심 통합 시나리오를 처음부터 끝까지 실행했다.
- [ ] `LOG`에서 부모가 자식보다 먼저 나왔다.
- [ ] `PATH c000002 c000003`이 공통 부모를 거쳤다.
- [ ] 독립 루트 커밋의 `PATH`가 `No path`를 출력했다.
- [ ] 키워드·작성자 SEARCH 결과가 맞았다.
- [ ] 정렬 옵션 두 가지가 실행됐다.
- [ ] 오류·경계값 입력이 프로그램을 종료시키지 않았다.
- [ ] `quit` 또는 `exit`으로 정상 종료했다.
